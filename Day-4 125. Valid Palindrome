class Solution {
    public boolean isPalindrome(String s) {
        String res=s.toLowerCase().replaceAll("[^a-z0-9]","");
        StringBuilder sb=new StringBuilder(res);
        String ans=sb.reverse().toString();
        boolean eq=res.equals(ans);
        return eq;
    }
}
